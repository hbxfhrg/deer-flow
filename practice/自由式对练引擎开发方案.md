# 自由式对练引擎开发方案

## 一、方案概述

基于 `pract_scene_set` 场景配置（知识库 + 考核维度 + 评分规则），实现 LLM 驱动的自由式对练。核心思路：用两次 LLM 调用驱动一轮对话——第一次扮演客户角色生成回复，第二次以教练身份对学员发言进行多维度打分。

### 整体架构

```
前端 ChatPage → useRoleplay hook → /api/roleplay/practice/start|turn|end
                                        ↓
                                  PracticeService
                                   ↙          ↘
                          LLM (客户角色)    LLM (评估打分)
                              ↓                ↓
                       pract_dialog_detail  pract_record
```

### 对练流程

```
用户进入 ChatPage → 点击"开始对练"
    ↓
POST /practice/start → 加载场景配置 → 创建 pract_course_record → LLM 生成客户开场白
    ↓
用户输入 → POST /practice/turn
    ↓
  保存用户话术 (pract_dialog_detail, speaker=1)
    ↓
  LLM 评估打分 (基于当前轮次考核维度 + scoring_rules + 对话历史)
    ↓
  写入 dialog_detail.score / .feedback
    ↓
  判断是否达到 total_rounds？
    ├─ 是 → is_complete=true，创建 pract_record，LLM 生成最终报告
    └─ 否 → LLM 生成下一条客户回复（聚焦下一个考核维度）→ 存 DB (speaker=2) → 返回前端
    ↓
用户也可以随时手动点击"结束对练" → POST /practice/end → 创建 pract_record → LLM 生成最终报告
```

### 结束条件（方案 C）

- A：达到场景配置的总轮次后自动结束（总轮次 = exam_categories 维度数量）
- B：用户手动点击"结束对练"按钮
- **两者都支持**：轮数到达自动触发结束，用户也可随时提前结束

---

## 二、数据库变更

### 2.1 `pract_course_record` 表（对练开始时创建）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增，作为 record_id |
| course_id | INT | 关联课程 ID |
| scene_id | INT | 关联场景 ID |
| start_time | DATETIME | 开始时间 |
| end_time | DATETIME | 结束时间（结束时更新） |
| total_score | INT | 总分（结束时更新） |
| user_name | VARCHAR(50) | 用户名 |
| nick_name | VARCHAR(200) | 昵称 |
| summary | TEXT | 总结报告（结束时更新） |
| conversation_id | VARCHAR(100) | 会话 ID |
| courese_name | VARCHAR(255) | 课程名称 |
| scene_name | VARCHAR(255) | 场景名称 |
| course_type | INT | 课程类型 |
| accord_finish | INT | 是否达标（0/1，结束时更新） |
| last_time | DATETIME | 最后活动时间 |
| practice_mode | VARCHAR(20) | 练习模式 |

### 2.2 `pract_record` 表（对练结束时创建）

```sql
-- =============================================
-- 自由式对练引擎：pract_record 新增字段
-- =============================================

-- 1. 添加课程关联字段
ALTER TABLE pract_record 
ADD COLUMN course_id BIGINT NOT NULL DEFAULT 0 COMMENT '关联的课程 ID';

-- 2. 添加报告数据字段
ALTER TABLE pract_record 
ADD COLUMN report_data JSON COMMENT '最终报告数据（JSON 格式，包含维度分数、优点、改进建议等）';

-- 3. 修改 avg_score 为 decimal 类型（当前是 int，不支持小数）
ALTER TABLE pract_record 
MODIFY COLUMN avg_score DECIMAL(5,2) DEFAULT NULL COMMENT '平均得分';
```

### 2.3 新建 `pract_dialog_detail` 表

```sql
-- =============================================
-- 自由式对练引擎：新建 pract_dialog_detail 表
-- =============================================
CREATE TABLE IF NOT EXISTS pract_dialog_detail (
    dialog_id    BIGINT      NOT NULL AUTO_INCREMENT  COMMENT '对话 ID，主键',
    record_id    INT         NOT NULL                 COMMENT '所属对练记录 ID（关联 pract_course_record.id）',
    speaker      TINYINT     NOT NULL                 COMMENT '发言者（1: 学员，2: AI/客户）',
    content_type TINYINT     NOT NULL DEFAULT 1       COMMENT '内容类型（1: 文本，2: 音频 URL）',
    content      TEXT         NOT NULL                COMMENT '发言内容（文本或音频 OSS 地址）',
    score        DECIMAL(5,2) DEFAULT NULL            COMMENT '该句得分（仅学员发言有值）',
    feedback     TEXT         DEFAULT NULL            COMMENT 'AI 对该句的即时反馈（仅学员发言有值）',
    create_time  DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '发言时间',
    PRIMARY KEY (dialog_id),
    INDEX idx_record_id (record_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='对话详情表';
```

**注意**：`round_number` 字段已移除，轮次通过对话历史动态计算。

---

## 三、后端开发

### 3.1 模型变更 (`models.py`)

#### 3.1.1 `PracticeRecordRow` 新增字段

```python
# pract_record 新增
course_id: Mapped[int] = mapped_column(BigInteger, nullable=False, default=0)
report_data: Mapped[dict] = mapped_column(JSON, default=dict)
# 修改 avg_score 类型（从 Integer 改为 Float）
avg_score: Mapped[float] = mapped_column(Float, nullable=True)
```

#### 3.1.2 新增 `DialogDetailRow`

```python
class DialogDetailRow(RoleplayBase):
    __tablename__ = "pract_dialog_detail"
    
    dialog_id: Mapped[int] = mapped_column(BigInteger, primary_key=True, autoincrement=True)
    record_id: Mapped[int] = mapped_column(Integer, nullable=False, index=True)  # 关联 pract_course_record.id
    speaker: Mapped[int] = mapped_column(Integer, nullable=False)  # 1: 学员, 2: AI/客户
    content_type: Mapped[int] = mapped_column(Integer, nullable=False, default=1)  # 1: 文本
    content: Mapped[str] = mapped_column(Text, nullable=False)
    score: Mapped[float] = mapped_column(Float, nullable=True)
    feedback: Mapped[str] = mapped_column(Text, nullable=True)
    create_time: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
```

#### 3.1.3 新增 `CourseRecordRow`

```python
class CourseRecordRow(RoleplayBase):
    __tablename__ = "pract_course_record"
    
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    course_id: Mapped[int] = mapped_column(Integer)
    scene_id: Mapped[int] = mapped_column(Integer)
    start_time: Mapped[datetime] = mapped_column(DateTime(timezone=True))
    end_time: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=True)
    total_score: Mapped[int] = mapped_column(Integer, nullable=True)
    user_name: Mapped[str] = mapped_column(String(50))
    nick_name: Mapped[str] = mapped_column(String(200), nullable=True)
    summary: Mapped[str] = mapped_column(Text, nullable=True)
    conversation_id: Mapped[str] = mapped_column(String(100), nullable=True)
    courese_name: Mapped[str] = mapped_column(String(255), nullable=True)  # 注意：字段名拼写与数据库一致
    scene_name: Mapped[str] = mapped_column(String(255), nullable=True)
    course_type: Mapped[int] = mapped_column(Integer, nullable=True)
    accord_finish: Mapped[int] = mapped_column(Integer, nullable=True)
    last_time: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=True)
    practice_mode: Mapped[str] = mapped_column(String(20), nullable=True)
```

### 3.2 新建 `practice_service.py`

文件路径：`backend/packages/harness/deerflow/roleplay/practice_service.py`

```python
class PracticeService:
    """自由式对练核心引擎"""
    
    @staticmethod
    async def start(course_id: int, user_name: str) -> dict:
        """
        开始对练：
        1. 加载课程和关联场景配置
        2. 创建 pract_course_record（对练开始时创建）
        3. 计算总轮次（自由式对练取 exam_categories 维度数量）
        4. 构造客户角色 system prompt（场景摘要 + 背景描述 + 角色设定 + 本轮考察重点）
        5. LLM 生成开场白（聚焦第一个考核维度）
        6. 开场白写入 pract_dialog_detail（speaker=2）
        7. 返回 record_id + 开场白内容
        """
    
    @staticmethod
    async def turn(record_id: int, user_message: str) -> dict:
        """
        处理一轮对话：
        1. 查询 pract_course_record 和场景配置
        2. 检查对练是否已结束（end_time 不为空则拒绝继续）
        3. 动态计算当前轮次（统计学员回复数量）
        4. 学员话术写入 pract_dialog_detail（speaker=1）
        5. 获取当前对话历史
        6. LLM 评估打分（聚焦当前轮次考核维度）
           → 返回：round_score, dimension_scores, feedback
        7. 写入 dialog_detail.score / .feedback
        8. 判断是否完成（current_round >= total_rounds）
           ├─ 是 → 创建 pract_record，进入结束流程，生成最终报告
           └─ 否 → LLM 生成下一轮客户回复（聚焦下一个考核维度）
        9. 客户回复写入 dialog_detail（speaker=2）
        10. 返回：customer_message, evaluation, is_complete, round
        """
    
    @staticmethod
    async def end(record_id: int) -> dict:
        """
        手动结束对练：
        1. 查询 pract_course_record 和对话历史
        2. 检查是否已结束（end_time 不为空则直接返回）
        3. 动态计算当前轮次（统计学员回复数量）
        4. LLM 生成最终评估报告（汇总各轮每维度评分取平均 + 优缺点 + 总结）
           → 返回：total_score, dimension_scores, strengths, improvements, summary
        5. 创建 pract_record（对练结束时创建）
        6. 更新 pract_course_record：end_time, total_score, summary, accord_finish
        7. 返回完整报告
        """
    
    @staticmethod
    async def get_practice_history(record_id: int) -> list[dict]:
        """获取某次对练的完整对话历史（含评分）"""
    
    @staticmethod
    async def get_report(record_id: int) -> dict:
        """获取最终评估报告"""
```

### 3.3 轮次计算逻辑

#### 3.3.1 总轮次计算

```python
# 总轮次计算逻辑：
# - 自由式对练（practice_mode="text"）：取 exam_categories 字段中逗号分隔的类别数量
# - 其他模式：使用 dialog_round_limit 配置，默认 5 轮
if scene.practice_mode == "text" and scene.exam_categories:
    # 同时支持中文逗号和英文逗号分割
    categories = scene.exam_categories.replace("，", ",").split(",")
    total_rounds = len([cat.strip() for cat in categories if cat.strip()])
    if total_rounds == 0:
        total_rounds = 5
else:
    total_rounds = scene.dialog_round_limit or 5
```

#### 3.3.2 当前轮次计算

```python
# 动态计算当前轮次：AI开场白不算轮次，统计学员回复的数量即为当前轮次
current_round = 0
for d in all_dialogs:
    if d.speaker == 1 and d.content:  # 学员回复
        current_round += 1

# 在 turn 方法中，学员回复后，当前轮次 + 1（这才是真正的当前轮次）
# current_round += 1  # 仅在 turn 方法中执行
```

**关键点说明**：
- AI 开场白（speaker=2）不计入轮次
- 每收到一条学员回复（speaker=1），轮次加1
- 在 `turn` 方法中，写入学员回复后需要再 +1（表示当前正在处理的轮次）
- 在 `end` 方法中，直接统计学员回复数量即可（无需额外 +1）

#### 3.3.3 获取当前轮次考核维度

```python
def _get_current_category(scene: SceneRow, round_num: int, seed: int = 0) -> str:
    """根据轮次获取当前需要考察的考核维度（使用 seed 保证同一次对练维度顺序一致）"""
    if not scene.exam_categories:
        return ""
    
    # 解析考核维度（支持中英文逗号）
    categories = scene.exam_categories.replace("，", ",").split(",")
    categories = [cat.strip() for cat in categories if cat.strip()]
    
    if not categories:
        return ""
    
    # 使用 seed 进行确定性随机打乱（保证同一次对练维度顺序一致）
    if seed != 0 and len(categories) > 1:
        rng = random.Random(seed)
        categories = categories.copy()
        rng.shuffle(categories)
    
    # 根据轮次返回对应的维度（轮次从1开始）
    index = round_num - 1
    if index < len(categories):
        return categories[index]
    
    # 如果轮次超过维度数量，返回最后一个维度
    return categories[-1]
```

### 3.4 LLM Prompt 设计

#### 3.4.1 客户角色 System Prompt

```
系统角色：你正在模拟一位真实客户，正在进行一场对话练习。

【场景背景】
{scene_description}

【场景知识】
{summary_text}

【你的人物设定】
- 说话自然、口语化，避免 AI 味
- 先回应对方上一条回答（简要总结或表达理解），再自然过渡到下一个问题
- 可以表现出犹豫、追问、表达疑虑或感兴趣
- 根据对方的回复自然地推进对话，保持对话连贯性
- 不要主动结束对话
- 你的回复要简短，2-4句话即可
- **关键要求**：提出**开放性问题**，鼓励学员详细阐述，不要问只能用"是"或"否"回答的问题

【当前对话轮次】：第 {round} / {total_rounds} 轮

【本轮考察重点】：{current_category}
- **硬性要求**：你的问题和对话内容**必须100%与此维度相关**，不允许谈论任何无关话题
- 你是来深入了解此维度的潜在客户，不是闲聊
- 提出与此维度直接相关的**具体、深入**的问题或疑虑
- 引导学员详细讲解、解释或推销此维度的特点、优势和价值
- 可以提出质疑、追问细节，促使学员充分展示专业知识
- 如果学员偏离话题，礼貌地将话题拉回到此维度上

例如，如果本轮考察"三电系统"，你可以问：
- "你能详细介绍一下这款车的电池技术吗？续航表现如何？"
- "充电速度怎么样？支持哪些充电方式？"
- "电机的动力性能如何？加速表现怎么样？"

例如，回应学员回答后再提问：
- 学员说："我们的车续航里程很长。"
- 你可以回答："续航长确实是个重要优势，能具体说说在不同路况下的实际表现吗？"

请根据以上设定，生成你对学员上一句话的回复。先简要回应，再提出相关问题。只输出回复内容，不要加任何前缀或说明。
```

#### 3.4.2 每轮评估 Prompt

```
系统角色：你是一位专业的对练教练，负责评估学员的对话表现。

【考核维度】（本次练习的全部考核维度）
{exam_categories}

【评分规则】（通用评分标准）
{scoring_rules}

【本轮考察重点】（当前轮次需要重点考察的维度）
{current_category}

【对话历史】（已完成的对话，含历史评分）
{dialog_history}

【学员刚才的发言】
{user_message}

请根据当前对话内容，对学员刚才的发言进行多维度评估。注意：
- 重点评估【本轮考察重点】中指定的维度
- 对每个考核维度独立打分（0-100 分），重点维度必须打分，其他维度如未涉及可不打分
- round_score 主要基于本轮重点考察维度的得分
- 按以下 JSON 格式输出（仅输出 JSON，不要其他内容）：

{
  "round_score": 85,
  "dimension_scores": {"产品知识": 80, "沟通技巧": 90},
  "feedback": "你的回复很好，抓住了客户的疑问点并给出了专业解释。建议下一步可以更主动地引导客户需求。"
}
```

#### 3.4.3 最终报告 Prompt

```
系统角色：你是一位专业的对练教练，请基于整场对话和每轮评分记录，生成最终评估报告。

【考核维度】（本次练习的全部考核维度）
{exam_categories}

【评分规则】（通用评分标准）
{scoring_rules}

【完整对话历史】（含每轮学员发言、AI 回复、每轮评分）
{full_dialog_with_scores}

请综合以上信息，生成完整的评估报告。要求：
- total_score 取所有维度分的平均值
- dimension_scores 列出每个考核维度的最终得分
- strengths 列出学员的突出优点
- improvements 列出需要改进的方向
- summary 给出整体评价

按以下 JSON 格式输出（仅输出 JSON）：

{
  "total_score": 82,
  "dimension_scores": {"产品知识": 80, "沟通技巧": 85, "问题解决": 80},
  "strengths": ["产品知识扎实", "善于倾听客户需求"],
  "improvements": ["需要加强异议处理技巧", "建议使用更多数据说服客户"],
  "summary": "整体表现良好，产品知识扎实..."
}
```

### 3.5 路由变更 (`roleplay.py`)

#### 3.5.1 新增 Pydantic 模型

```python
class PracticeStartRequest(BaseModel):
    course_id: int = Field(alias="courseId")
    user_name: str = Field(alias="userName")

class PracticeTurnRequest(BaseModel):
    record_id: int = Field(alias="recordId")
    message: str

class PracticeEndRequest(BaseModel):
    record_id: int = Field(alias="recordId")
```

#### 3.5.2 新增路由

```python
@router.post("/practice/start", summary="开始对练")
async def practice_start(req: PracticeStartRequest):
    """加载场景 → 创建 pract_course_record → LLM生成开场白"""
    result = await PracticeService.start(req.course_id, req.user_name)
    return result

@router.post("/practice/turn", summary="对练对话轮次")
async def practice_turn(req: PracticeTurnRequest):
    """存用户话术 → LLM评估 → LLM生成下一条客户回复"""
    result = await PracticeService.turn(req.record_id, req.message)
    return result

@router.post("/practice/end", summary="结束对练")
async def practice_end(req: PracticeEndRequest):
    """手动结束 → 创建 pract_record → LLM生成最终报告"""
    result = await PracticeService.end(req.record_id)
    return result

@router.get("/practice/{record_id}/history", summary="获取对练历史")
async def practice_history(record_id: int):
    return await PracticeService.get_practice_history(record_id)

@router.get("/practice/{record_id}/report", summary="获取评估报告")
async def practice_report(record_id: int):
    return await PracticeService.get_report(record_id)
```

### 3.6 API 响应格式（驼峰命名）

#### POST /practice/start 响应

```json
{
  "recordId": 1,
  "courseName": "销售对练课程",
  "sceneName": "至境 E7 展厅接待",
  "sceneDescription": "模拟展厅接待场景...",
  "totalRounds": 3,
  "customerMessage": "您好，我对你们的新品比较感兴趣，能介绍一下产品知识吗？",
  "round": 1
}
```

#### POST /practice/turn 响应

```json
{
  "recordId": 1,
  "round": 2,
  "evaluation": {
    "roundScore": 85,
    "dimensionScores": {"产品知识": 80, "沟通技巧": 90},
    "feedback": "你的回复很好..."
  },
  "customerMessage": "嗯，听起来不错，那沟通方面你们是怎么做的呢？",
  "isComplete": false
}
```

当 `isComplete=true` 时额外返回：

```json
{
  "isComplete": true,
  "report": {
    "totalScore": 82,
    "dimensionScores": {...},
    "strengths": ["..."],
    "improvements": ["..."],
    "summary": "..."
  },
  "message": "本次对练已结束，稍后查看整个结果"
}
```

#### POST /practice/end 响应

```json
{
  "recordId": 1,
  "totalRounds": 3,
  "report": {
    "totalScore": 82,
    "dimensionScores": {...},
    "strengths": ["..."],
    "improvements": ["..."],
    "summary": "..."
  },
  "message": "本次对练已结束，稍后查看整个结果"
}
```

---

## 四、前端开发

### 4.1 类型定义 (`types/index.ts`)

```typescript
// 新增：对练 API 响应类型（驼峰命名）
export interface PracticeStartResponse {
  recordId: number;
  courseName: string;
  sceneName: string;
  sceneDescription: string;
  totalRounds: number;
  customerMessage: string;
  round: number;
}

export interface PracticeTurnResponse {
  recordId: number;
  round: number;
  evaluation: {
    roundScore: number;
    dimensionScores: Record<string, number>;
    feedback: string;
  };
  customerMessage: string;
  isComplete: boolean;
  report?: EvaluationReport;  // 完成时附带
  message?: string;  // 结束提示
}

export interface PracticeEndResponse {
  recordId: number;
  totalRounds: number;
  report: EvaluationReport;
  message: string;
}

export interface EvaluationReport {
  totalScore: number;
  dimensionScores: Record<string, number>;
  strengths: string[];
  improvements: string[];
  summary: string;
}
```

### 4.2 API 方法 (`api/index.ts`)

```typescript
// 新增 practice 接口
practice: {
  async start(courseId: number, userName: string): Promise<PracticeStartResponse> {
    const response = await axiosInstance.post('/roleplay/practice/start', {
      courseId,
      userName,
    });
    return response.data;
  },
  
  async turn(recordId: number, message: string): Promise<PracticeTurnResponse> {
    const response = await axiosInstance.post('/roleplay/practice/turn', {
      recordId,
      message,
    });
    return response.data;
  },
  
  async end(recordId: number): Promise<PracticeEndResponse> {
    const response = await axiosInstance.post('/roleplay/practice/end', {
      recordId,
    });
    return response.data;
  },
},
```

### 4.3 重写 `useRoleplay.ts`

核心改动：
- **删除**：thread/run/SSE 相关的所有代码
- **替换为**：直接调用 `api.practice.start/turn/end` 的普通 HTTP 请求
- **消息管理**：前端本地维护 message 数组（role='user'/'assistant'）
- **评估管理**：每轮 turn 响应中携带 evaluation，直接更新
- **轮次管理**：后端返回当前轮次，前端直接显示

```typescript
export function useRoleplay(courseId: number | null) {
  const [messages, setMessages] = useState<Message[]>([]);
  const [recordId, setRecordId] = useState<number | null>(null);
  const [isLoading, setIsLoading] = useState(false);
  const [isTyping, setIsTyping] = useState(false);
  const [currentRound, setCurrentRound] = useState(0);
  const [totalRounds, setTotalRounds] = useState(0);
  const [evaluation, setEvaluation] = useState<Evaluation | null>(null);
  const [report, setReport] = useState<EvaluationReport | null>(null);
  const [isComplete, setIsComplete] = useState(false);
  const messagesEndRef = useRef<HTMLDivElement>(null);

  // initConversation: 调用 api.practice.start
  const initConversation = useCallback(async (userName: string) => {
    if (!courseId) return;
    setIsLoading(true);
    try {
      const res = await api.practice.start(courseId, userName);
      setRecordId(res.recordId);
      setTotalRounds(res.totalRounds);
      setCurrentRound(1);
      const aiMsg: Message = {
        id: `ai-${Date.now()}`,
        role: 'assistant',
        content: res.customerMessage,
        createdAt: new Date().toISOString(),
      };
      setMessages([aiMsg]);
    } catch (e) {
      console.error('Failed to start practice:', e);
    } finally {
      setIsLoading(false);
    }
  }, [courseId]);

  // sendMessage: 调用 api.practice.turn
  const sendMessage = useCallback(async (content: string) => {
    if (!recordId || isLoading || content.trim() === '' || isComplete) return;
    
    const userMsg: Message = {
      id: `user-${Date.now()}`,
      role: 'user',
      content: content.trim(),
      createdAt: new Date().toISOString(),
    };
    setMessages(prev => [...prev, userMsg]);
    setIsLoading(true);
    setIsTyping(true);

    try {
      const res = await api.practice.turn(recordId, content.trim());
      
      // 更新评估
      if (res.evaluation) {
        setEvaluation({
          round: res.round,
          totalScore: res.evaluation.roundScore,
          dimensionScores: res.evaluation.dimensionScores,
          strengths: [],
          improvements: [],
          summary: res.evaluation.feedback,
        });
      }
      
      // 客户回复
      if (res.customerMessage) {
        const aiMsg: Message = {
          id: `ai-${Date.now()}`,
          role: 'assistant',
          content: res.customerMessage,
          createdAt: new Date().toISOString(),
        };
        setMessages(prev => [...prev, aiMsg]);
      }
      
      setCurrentRound(res.round);
      
      if (res.isComplete) {
        setIsComplete(true);
        if (res.report) setReport(res.report);
      }
    } catch (e) {
      console.error('Failed to send message:', e);
    } finally {
      setIsLoading(false);
      setIsTyping(false);
    }
  }, [recordId, isLoading, isComplete]);

  // endPractice: 调用 api.practice.end
  const endPractice = useCallback(async () => {
    if (!recordId) return;
    setIsLoading(true);
    try {
      const res = await api.practice.end(recordId);
      setIsComplete(true);
      setReport(res.report);
    } catch (e) {
      console.error('Failed to end practice:', e);
    } finally {
      setIsLoading(false);
    }
  }, [recordId]);

  return {
    messages, isLoading, isTyping, evaluation, report,
    isComplete, currentRound, totalRounds,
    initConversation, sendMessage, endPractice, messagesEndRef,
  };
}
```

### 4.4 `ChatPage.tsx` 改动

改动点：
1. `useRoleplay` 传入 `courseId` 参数和 `userName`
2. 新增结束按钮（手动结束对练）
3. 达到 totalRounds 后禁用输入，显示结束状态和提示信息
4. 报告展示：用独立面板展示 `report` 数据（总分 + 雷达图 + 优缺点 + 总结）

```tsx
// 核心改动：
const { ..., endPractice, report, isComplete, currentRound, totalRounds } = useRoleplay(courseId ? Number(courseId) : null);

// 新增结束按钮（在输入框旁边或顶部）
{!isComplete && messages.length > 0 && (
  <button onClick={endPractice} disabled={isLoading}>结束对练</button>
)}

// 结束后展示报告和提示
{isComplete && (
  <div>
    <p>本次对练已结束，稍后查看整个结果</p>
    {report && <ReportPanel report={report} />}
  </div>
)}

// 禁用输入
<textarea 
  disabled={isComplete || isLoading}
  placeholder={isComplete ? '对练已结束' : '输入您的回复...'}
/>
```

---

## 五、需要新增/修改的文件清单

| # | 文件 | 操作 | 说明 |
|---|------|------|------|
| 1 | `backend/scripts/migrate_practice_v2.sql` | **新建** | DB 迁移脚本 |
| 2 | `backend/packages/harness/deerflow/roleplay/models.py` | 修改 | 新增 `DialogDetailRow`、`CourseRecordRow`，`PracticeRecordRow` 加字段 |
| 3 | `backend/packages/harness/deerflow/roleplay/practice_service.py` | **新建** | 核心对练引擎 |
| 4 | `backend/packages/harness/deerflow/roleplay/services.py` | 修改 | 新增 `DialogDetailService` 基础 CRUD |
| 5 | `backend/app/gateway/routers/roleplay.py` | 修改 | 新增 5 个 practice 路由 + Pydantic 模型 |
| 6 | `backend/app/gateway/auth_middleware.py` | 修改 | 将 `/api/roleplay/practice/` 添加到公共路径 |
| 7 | `practice/src/types/index.ts` | 修改 | 新增对练 API 响应类型（驼峰命名） |
| 8 | `practice/src/api/index.ts` | 修改 | 新增 `practice.start/turn/end/history/report` |
| 9 | `practice/src/hooks/useRoleplay.ts` | **重写** | 替换为直接 HTTP 调用模式 |
| 10 | `practice/src/pages/ChatPage.tsx` | 修改 | 接入新 hook，添加结束按钮和报告展示 |
| 11 | `practice/src/components/ReportPanel.tsx` | **新建** | 对练报告展示组件 |

---

## 六、实现顺序

1. **数据库迁移**（建表 + 改字段）
2. **模型层**（`DialogDetailRow` + `CourseRecordRow` + `PracticeRecordRow` 改字段）
3. **`practice_service.py`**（核心引擎三大方法 + 轮次计算逻辑）
4. **`services.py`**（`DialogDetailService` CRUD）
5. **`roleplay.py` 路由**（5 个新端点）
6. **`auth_middleware.py`**（添加公共路径）
7. **前端 types** → **api** → **hook** → **ChatPage** → **ReportPanel**

---

## 七、关键设计变更总结

### 7.1 数据库设计变更

| 表名 | 创建时机 | 作用 |
|------|---------|------|
| `pract_course_record` | **对练开始时** | 记录对练基本信息 |
| `pract_record` | **对练结束时** | 记录对练统计结果（总分、报告等） |
| `pract_dialog_detail` | 每轮对话 | 记录对话详情 |

### 7.2 轮次计算逻辑

| 计算项 | 逻辑 |
|--------|------|
| 总轮次 | 自由式对练取 `exam_categories` 维度数量（支持中英文逗号分割），其他模式取 `dialog_round_limit`，默认5轮 |
| 当前轮次 | 动态计算：统计学员回复数量（AI开场白不计入），`turn`方法中学员回复后+1 |
| 每轮维度 | **随机顺序**：使用 `record_id` 作为随机种子打乱 `exam_categories` 顺序，同一次对练维度顺序一致，无需存储 |

### 7.3 核心特性

- ✅ 每轮对话聚焦到一个指定考核维度
- ✅ 模拟客户主动引导学员讨论该维度内容
- ✅ 评估时重点关注当前轮次维度
- ✅ 对练结束后禁止继续输入（在 `_load_scene_by_record` 中检查 `end_time`）
- ✅ 返回结束提示信息
- ✅ **对话自然衔接**：先回应学员上一条回答，再自然过渡到下一个问题
- ✅ **随机维度顺序**：对练开始时随机打乱考核维度顺序，确保5轮对话覆盖所有维度

### 7.4 修复的 Bug 列表

| Bug 描述 | 修复位置 | 修复说明 |
|----------|---------|---------|
| 前后端字段命名不一致 | `practice_service.py` | 返回字典统一使用驼峰命名（`recordId`、`courseName` 等） |
| 异步调用缺少 `await` | `practice_service.py` | 在调用 `_load_scene_by_course` 和 `_load_scene_by_record` 时添加 `await` |
| 轮次计算错误（AI回复算轮次） | `practice_service.py` | 改为统计学员回复数量确定当前轮次 |
| 对练结束后仍允许输入 | `_load_scene_by_record` | 添加 `end_time` 检查，已结束则拒绝继续 |
| 客户未聚焦考核维度提问 | `CUSTOMER_SYSTEM_PROMPT` | 强化 Prompt，添加硬性要求"问题必须100%与此维度相关" |
| `pract_course_record` 未生成 | `start` 方法 | 在对练开始时创建 `CourseRecordRow` 记录 |
| `end` 方法轮次计算不一致 | `end` 方法 | 统一使用统计学员回复数量的逻辑 |
| **对话衔接生硬（未回应学员回答）** | `CUSTOMER_SYSTEM_PROMPT` | 添加要求：先回应学员上一条回答（简要总结或表达理解），再自然过渡到下一个问题 |
| **维度顺序固定** | `practice_service.py` | 使用 `record_id` 作为随机种子打乱考核维度顺序，同一次对练维度顺序一致，确保5轮对话覆盖所有维度，无需修改数据库 |

---

## 八、智能错误处理机制

### 8.1 问题背景

在对练过程中，LLM（大语言模型）可能返回格式不正确的 JSON 数据，导致解析失败。为确保系统稳定性，后端实现了**智能环形重试机制**。

### 8.2 重试策略

**每轮按优化后的顺序执行修复策略，最多走3轮：**

```
基础解析失败
    ↓
识别错误类型（如：missing_quote）
    ↓
获取优化策略顺序（如：llm_regen → llm_fix → py_fix）
    ↓
第1轮：按顺序执行策略
    ↓ (全部失败)
第2轮：按顺序执行策略
    ↓ (全部失败)
第3轮：按顺序执行策略
    ↓ (全部失败)
返回错误提示："程序出错了，请稍后再试。"
```

### 8.3 错误类型识别

| 错误类型 | 识别特征 | 说明 |
|----------|----------|------|
| `missing_quote` | `property name`、`double quote` | 键名缺少引号 |
| `missing_comma` | `expecting ','` | 缺少逗号分隔符 |
| `missing_bracket` | `unexpected end of json` | 缺少闭合括号 |
| `format_mess` | `invalid`、`multiple` | 格式混乱 |
| `content_empty` | `null`、`empty` | 数据为空 |
| `unknown` | 其他 | 无法识别的错误 |

### 8.4 智能步骤排序

**策略成功率配置**：
```python
_REPAIR_STRATEGY_SUCCESS_RATES = {
    "py_fix": 0.75,    # Python代码修复成功率
    "llm_fix": 0.85,   # LLM格式修复成功率
    "llm_regen": 0.9,  # LLM重新生成成功率
}
```

**排序算法**：
1. 根据错误类型获取推荐策略列表
2. 结合成功率进行二次排序
3. 按成功率从高到低执行

### 8.5 核心函数

| 函数名 | 功能说明 |
|--------|----------|
| `_classify_json_error` | 识别 JSON 解析错误类型 |
| `_get_optimized_strategy_order` | 根据错误类型和成功率生成最优策略顺序 |
| `_fix_json_with_code` | Python 代码修复：修复缺引号、缺逗号、缺括号等 |
| `_fix_json_format` | LLM 格式修复：将报错 JSON 送入 LLM 修复 |
| `_regenerate_json` | LLM 重新生成：将完整会话送入 LLM 重新生成 |

---

## 九、对练结束语生成

### 9.1 功能说明

对练正常结束时（所有轮次完成），自动生成客户结束语，使对话自然收尾。

### 9.2 实现逻辑

```python
async def _generate_closing_message(scene: SceneRow, dialogs: list, total_score: float) -> str:
    """生成对练结束语"""
    # 调用 LLM 生成自然友好的结束语
    # 包含：感谢学员解答、表达对产品的兴趣、期待下次交流
    # 返回：结束语文本
```

### 9.3 调用时机

在 `turn` 方法中，当 `is_complete=True` 时调用：

```python
if is_complete:
    # 生成结束语
    closing_message = await _generate_closing_message(scene, all_dialogs, total_score)
    
    # 保存结束语到数据库
    await DialogDetailService.create_dialog({
        "record_id": record_id,
        "speaker": 2,  # 客户
        "content": closing_message,
    })
    
    # 返回结束语给前端
    return {
        ...
        "customerMessage": closing_message,
        "isComplete": True,
        ...
    }
```

### 9.4 结束语示例

- "感谢你的详细介绍！我对这款产品很感兴趣，考虑一下后会联系你。再见！"
- "谢谢你的耐心解答，我了解得很清楚了。期待下次见面！"

---

## 十、评价展示优化

### 10.1 功能说明

评价结果嵌入用户消息气泡内部，默认只显示"改进建议"按钮，点击后展开详细评价内容。

### 10.2 前端修改

#### 10.2.1 MessageBubble.tsx 改动

```tsx
{!isEvaluating && message.evaluation && (
  <>
    {/* 默认状态：只显示重录和改进建议按钮 */}
    <div className="flex items-center justify-between gap-2 mt-2 pt-2 border-t border-gray-100">
      {onRetry && (
        <button onClick={onRetry}>
          <RotateCcw className="w-4 h-4" />
          重录
        </button>
      )}
      <button onClick={() => setIsExpanded(!isExpanded)}>
        <Pencil className="w-4 h-4" />
        {isExpanded ? '收起建议' : '改进建议'}
      </button>
    </div>

    {/* 展开的评价详情 */}
    {isExpanded && (
      <div className="mt-2 pt-2 border-t border-gray-100 bg-gray-50 px-4 py-3 rounded-b-xl">
        {/* 评价详情内容：分数、星级、维度得分、总结、改进建议、润色表达 */}
      </div>
    )}
  </>
)}
```

#### 10.2.2 Message 类型扩展

```typescript
export interface Message {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  createdAt?: string;
  evaluation?: MessageEvaluation;
  isEvaluating?: boolean;  // 是否正在评价中
  roundNumber?: number;     // 当前轮次
}
```

#### 10.2.3 useRoleplay.ts 改动

在发送消息时设置 `isEvaluating: true`，收到响应后更新为 `isEvaluating: false`：

```typescript
const userMsg: Message = {
  id: `user-${Date.now()}`,
  role: 'user',
  content: content.trim(),
  createdAt: new Date().toISOString(),
  isEvaluating: true,  // 标记正在评价中
  roundNumber: currentRound,
};
```

#### 10.2.4 ChatPage.tsx 改动

移除底部重复的"评估结果"面板：

```tsx
// 移除移动端过程评估面板
{/* {evaluation && !isComplete && (
  <div className="lg:hidden bg-white border-t border-gray-100 p-4">
    <EvaluationCard evaluation={evaluation} status="completed" />
  </div>
)} */}
```

### 10.3 交互流程

```
用户发送消息
    ↓
设置 isEvaluating: true（显示"评价生成中"状态）
    ↓
后端返回评价结果
    ↓
设置 isEvaluating: false，保存评价数据
    ↓
显示"改进建议"按钮（默认不展开）
    ↓
用户点击按钮 → 展开详细评价内容
```

---

## 十一、练习历史功能

### 11.1 功能说明

显示用户的对练记录列表，支持按时间范围和状态筛选，点击进行中的记录可继续未完成的对话。

### 11.2 页面布局

导航栏位置：开始练习 → 练习历史

### 11.3 筛选功能

| 筛选条件 | 说明 | 默认值 |
|----------|------|--------|
| 时间范围 | 起止时间，最多3天 | 最近3天 |
| 状态 | 全部/已完成/进行中 | 全部 |

### 11.4 API 接口

```python
@router.get("/practice/records", summary="获取对练记录列表")
async def get_practice_records(
    user_name: str,
    start_time: Optional[datetime] = None,
    end_time: Optional[datetime] = None,
    status: Optional[str] = None  # all/completed/in_progress
):
    return await PracticeService.get_records(user_name, start_time, end_time, status)
```

### 11.5 继续对练逻辑

点击进行中的记录 → 加载对话历史 → 继续对话：

```typescript
// 获取历史记录
const records = await api.practice.getRecords(userName, startTime, endTime, status);

// 点击继续
const continueRecord = (recordId: number) => {
    navigate(`/chat/${recordId}`);
};
```

---

## 十二、对练首句优化

### 12.1 功能说明

对练开始时，客户首句添加自我介绍内容，避免突兀。

### 12.2 Prompt 修改

在 `CUSTOMER_SYSTEM_PROMPT` 中添加要求：

```python
CUSTOMER_SYSTEM_PROMPT = """
你是一位电动汽车潜在客户...

对话开始时，请先做简短自我介绍（如：我是来了解新能源车的客户），然后再提出问题。
自我介绍要自然，不要过于正式。

示例开场白：
- "你好，我对你们的新车挺感兴趣的，想了解一下产品信息。"
- "您好！我正在考虑换一辆新能源车，能介绍一下吗？"
"""
```

### 12.3 开场白示例

- "你好，我对你们的新品比较感兴趣，能介绍一下产品知识吗？"
- "您好！我正在考虑买新能源车，想了解一下续航方面的情况。"

---

## 十三、练习课程删除功能

### 13.1 功能说明

支持物理删除练习课程（无效课程才是改状态）。

### 13.2 前端修改

在 `CoursePage.tsx` 中添加删除按钮：

```tsx
<button
  onClick={() => handleDelete(course.id)}
  className="text-red-500 hover:text-red-700"
>
  <Trash2 className="w-4 h-4" />
</button>
```

### 13.3 API 接口

```python
@router.delete("/courses/{course_id}", summary="删除课程")
async def delete_course(course_id: int):
    return await CourseService.delete(course_id)
```

### 13.4 删除逻辑

```python
async def delete(course_id: int) -> dict:
    """物理删除课程记录"""
    async with async_session() as session:
        # 删除关联的对练记录
        await session.execute(
            delete(PractCourseRecord).where(PractCourseRecord.course_id == course_id)
        )
        
        # 删除课程
        await session.execute(
            delete(PractCourse).where(PractCourse.course_id == course_id)
        )
        
        await session.commit()
    
    return {"success": True, "message": "课程删除成功"}
```

---

## 十四、待确认事项

1. **user_name 来源**：当前从前端传入，是否 OK？
2. **LLM 模型选择**：使用场景配置的 `model_name` 字段还是系统默认模型？（建议：优先场景配置，无则走系统默认）
3. **语音对练**：一期仅实现文本对练，语音输入后续扩展？

> **确认后开始按顺序实现，预计涉及约 11 个文件的新增/修改。**
